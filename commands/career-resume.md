---
description: Turn recorded evidence into resume bullets
argument-hint: "[role or period to focus on]"
---

Use the `career-memory` skill, following `references/outputs.md`.

Generate resume bullets from career memory, focused on: $ARGUMENTS

One line each: strong verb, what was done, measurable result — but only using
numbers the user actually recorded, or arithmetic derived from them. Save to
`outputs/resume.md`.
