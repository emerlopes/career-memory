---
description: Build a promotion case for a target role
argument-hint: "[target role, e.g. Staff Engineer]"
---

Use the `career-memory` skill, following `references/outputs.md`.

Build a promotion case for: $ARGUMENTS (if empty, read the target from
`profile.md`, and ask if it is not there).

Map recorded evidence to the criteria for that level, show how often each
pattern appears, and be specific about what is thin or missing — the gaps are
the most useful part. Do not deliver a verdict on readiness unless the user
explicitly asks for your opinion. Save to `outputs/promotion-case.md`.
