---
description: Search your career memory
argument-hint: "[what to look for]"
---

Use the `career-memory` skill.

Search career memory for: $ARGUMENTS

Combine `search` with `--window`, `--project`, `--type` and `--skill` as the
question implies. Summarize what you found with dates and evidence, and link to
the entry files. If nothing matches, say so — do not answer from your own
recollection of this conversation.
