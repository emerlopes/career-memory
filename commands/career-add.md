---
description: Record something you did into your career memory
argument-hint: "[what you did]"
---

Use the `career-memory` skill.

Capture this into career memory: $ARGUMENTS

Follow the capture workflow: extract only what was actually stated, classify it,
ask at most one high-value clarifying question, write it with the CLI, and
confirm briefly. If `$ARGUMENTS` is empty, ask what they want to record.

Do not invent metrics, outcomes or evidence that were not given.
