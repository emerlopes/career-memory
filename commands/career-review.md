---
description: Draft a performance review from your career memory
argument-hint: "[period, e.g. this-year]"
---

Use the `career-memory` skill, following `references/outputs.md`.

Draft a performance review covering: $ARGUMENTS (default to the current review
period, or the last six months if unknown).

Read `profile.md` first — if it names the company's ladder or values, organize
against those. Every claim must trace to a recorded entry. Where a section has
no supporting evidence, say so plainly instead of padding it. Save to
`outputs/performance-review.md` and tell the user the path.
